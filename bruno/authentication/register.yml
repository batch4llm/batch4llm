info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/authentication/register"
  body:
    type: json
    data: |-
      {
        "username": "test",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
