info:
  name: get_batch
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/batches/:id"
  params:
    - name: id
      value: "{{batch_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
