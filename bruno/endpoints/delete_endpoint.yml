info:
  name: delete_endpoint
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/endpoints/delete/:id"
  params:
    - name: id
      value: "66"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
