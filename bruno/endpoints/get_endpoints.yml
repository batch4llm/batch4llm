info:
  name: get_endpoints
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/endpoints"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 200) {
          var body = res.getBody();
          if (body.length > 0) {
            var newId = body[0].id;
            bru.setVar("endpoint_id", newId);
          }
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
