info:
  name: get_models
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/endpoints/models/:id"
  params:
    - name: id
      value: "{{endpoint_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
