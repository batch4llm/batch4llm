info:
  name: get_file_readers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/pipeline/file_readers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
