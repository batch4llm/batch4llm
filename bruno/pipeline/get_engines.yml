info:
  name: get_engines
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/pipeline/engines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
