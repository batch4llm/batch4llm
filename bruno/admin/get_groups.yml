info:
  name: get_groups
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/admin/groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
