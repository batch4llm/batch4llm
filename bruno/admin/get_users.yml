info:
  name: get_users
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
