info:
  name: delete_prompt
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/prompts/delete/:id"
  params:
    - name: id
      value: "56"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
